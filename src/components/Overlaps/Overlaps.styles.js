import styled from 'styled-components';

export const Overlaps = styled.div`
    display: block;
    position: absolute;
    top: 0;
    width: 100%;
    height: 100%;
    background: rgba(20, 20, 20, 0.85);
    z-index: 9;
`;
